How to create a new project:
	- Create a new folder under packages
	- Copy this directory excluding node_modules if it exists to the folder
	- Rename the public.html title, package.json name and app.tsx NavBar title
	- Create a suitable Dockerfile and DockerStack file
	- Create a backend in HaProxy and map it to a URL of your choice
	